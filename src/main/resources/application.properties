spring.application.name=demo-backend

# ??Docker Compose????
spring.docker.compose.enabled=true

# PostgreSQL ??
spring.datasource.url=jdbc:postgresql://localhost:15432/myapp_db
spring.datasource.username=myapp_user
spring.datasource.password=myapp_password
spring.datasource.driver-class-name=org.postgresql.Driver

# Jimmer ORM配置
jimmer.dialect=org.babyfish.jimmer.sql.dialect.PostgresDialect
jimmer.show-sql=true
jimmer.pretty-sql=true
jimmer.database-validation-mode=ERROR

# JWT??
app.jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
app.jwt.expiration=86400000

# ????
logging.level.com.xdw.demobackend=DEBUG
logging.level.org.springframework.security=DEBUG

# ?????
server.port=8080

# ????
app.cors.allowed-origins=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000

# ????
# app.cors.allowed-origins=https://yourdomain.com,https://www.yourdomain.com

# JPA配置 - Disabled: Eager loading via Fetcher API handles all association loading
# spring.jpa.open-in-view=false is the default and recommended value
# See: .sisyphus/notepads/mybatis-to-jimmer-migration/eager-loading-implementation.md
